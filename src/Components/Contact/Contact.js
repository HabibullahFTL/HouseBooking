import React from 'react';
import Navbar from '../Navbar/Navbar';

const Contact = () => {
    return (
        <section className="row">
            <div className="container">
                <Navbar></Navbar>
                <h2 className="bg-dark">Contact</h2>
            </div>
        </section>
    );
};

export default Contact;